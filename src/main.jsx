import React from "react";
import ReactDOM from "react-dom/client";

const myFirstElement = <h1>This is frist element</h1>;

ReactDOM.createRoot(document.getElementById("root")).render(myFirstElement);